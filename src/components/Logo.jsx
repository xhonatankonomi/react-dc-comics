import logo from '../assets/dc-logo.png'

const Logo = () => {
    return (
        <div className="logo-container">
            <img src={logo} alt="dc-logo" />
        </div>
    );
};

export default Logo;